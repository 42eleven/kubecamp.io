baseURL = 'https://www.kubecamp.io'
languageCode = 'en-us'
title = 'KubeCamp.io'
theme = "terminal"

[module]
  [[module.imports]]
    path = "github.com/hugomods/images"

[markup.goldmark.renderer]
  unsafe = true

[params]
  # dir name of your main content (default is `content/posts`).
  # the list of set content will show up on your index page (baseurl).
  contentTypeName = "events"

  # ["orange", "blue", "red", "green", "pink"]
  themeColor = "blue"

  # if you set this to 0, only submenu trigger will be visible
  showMenuItems = 3

  # show selector to switch language
  showLanguageSelector = false

  fullWidthTheme = true

  # center theme with default width
  centerTheme = true

  # if your resource directory contains an image called `cover.(jpg|png|webp)`,
  # then the file will be used as a cover automatically.
  # With this option you don't have to put the `cover` param in a front-matter.
  autoCover = true

  # set post to show the last updated
  # If you use git, you can set `enableGitInfo` to `true` and then post will automatically get the last updated
  showLastUpdated = false

  # favicon = "favicon.ico"

  # set all headings to their default size (depending on browser settings)
  # oneHeadingSize = true

  # whether to show a page's estimated reading time
  # readingTime = false

  # whether to show a table of contents
  # can be overridden in a page's front-matter
  # Toc = false # default

[languages]
  [languages.en]
    languageName = "English"
    title = "KubeCamp"
    subtitle = "A 2d instructor-led Kubernetes Bootcamp for power developers and administrators"
    owner = ""
    keywords = "cloud, k8s, kubernetes, sre, bootcamp, training"
    copyright = "2023 © FortyTwo Eleven Consulting AB"
    menuMore = "Show more"
    readMore = "Read more"
    readOtherPosts = "Read other posts"
    newerPosts = "Newer posts"
    olderPosts = "Older posts"
    missingContentMessage = "Page not found..."
    missingBackButtonLabel = "Back to home page"

    [languages.en.params.logo]
      logoText = "KubeCamp"
      logoHomeLink = "/"

    [languages.en.menu]
      [[languages.en.menu.main]]
        identifier = "agenda"
        name = "Agenda"
        url = "/pages/agenda"
    [[languages.en.menu.main]]
        identifier = "instructors"
        name = "Instructors"
        url = "/pages/instructors"
    [[languages.en.menu.main]]
        identifier = "faq"
        name = "FAQ"
        url = "/pages/faq"
